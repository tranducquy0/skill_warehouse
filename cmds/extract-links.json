{
    "name": "extract-links",
    "desc": "Extract all URLs from the current page",
    "prompt": "Find all hyperlinks on the current page and list them as plain text, one per line. Filter out any internal anchors (#) and duplicates."
}
